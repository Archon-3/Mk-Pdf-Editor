import type { ToolDefinition } from '../../shared/types'

export const toolMeta: ToolDefinition = {
  id: 'pdf-to-powerpoint', name: 'PDF → PowerPoint', description: 'Convert PDF pages into editable PowerPoint slides.', category: 'convert', endpoint: '/tools/pdf-to-powerpoint',
}